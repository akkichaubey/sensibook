import React, { useState } from "react";
import { Link } from "react-router-dom";

const Header = () => {
  return (
    <>
      <header className="header bg-black">
        <div className="container">
          <h2 className="text-white text-center font-medium">Header</h2>
        </div>
      </header>
    </>
  );
};

export default Header;
